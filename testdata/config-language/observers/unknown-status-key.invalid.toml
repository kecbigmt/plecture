# plect-fixture: result=invalid layer=semantic diagnostic=PLECT-CFG-FROM-PATH entry=definitions
# reason: a resource-status projection names a key the resolved observer's state schema does not declare.

[review]
kind     = "task"
requires = ["checks"]

[review.setup]
type = "exec"
bin  = "github-issue-pr"
args = ["render-instruction"]

[review.outputs.bind]
checks = { from = "resource.status.check_status" }

[review.outputs_schema]
type = "object"

[review.outputs_schema.properties]
checks = { type = "string", mutable = true }
