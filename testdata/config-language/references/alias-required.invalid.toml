# plect-fixture: result=invalid layer=semantic diagnostic=PLECTURE-CFG-REF-ALIAS-REQUIRED entry=definitions
# reason: in user-owned config a relative reference that would select catalog content must carry its catalog alias.

[my_session]
kind               = "workflow"
workspace_provider = "worktree"

[[my_session.nodes]]
uses = "runtime"
