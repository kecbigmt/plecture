# plect-fixture: result=invalid layer=semantic diagnostic=PLECT-CFG-KIND-MISMATCH entry=definitions
# reason: a workflow node is a position in a lifecycle graph, so it references a task; work arrives afterward, against the session that graph produced.

[review_session]
kind = "workflow"

[[review_session.nodes]]
uses = "review"
