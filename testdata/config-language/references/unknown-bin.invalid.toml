# plect-fixture: result=invalid layer=semantic diagnostic=PLECT-CFG-BIN-UNKNOWN entry=definitions
# reason: a bare bin name resolves against the containing plugin's own executables, and this plugin declares no such executable.

[bootstrap]
kind = "task"

[bootstrap.setup]
type = "exec"
bin  = "okf-goal-absent"
args = ["task", "bootstrap"]
