# plect-fixture: result=invalid layer=structural diagnostic=PLECTURE-CFG-VALUE-TAG-UNKNOWN entry=definitions
# reason: fallback is not part of the tagged-value vocabulary.

[bootstrap]
kind = "effect"

[bootstrap.setup]
type = "exec"
bin  = "okf-goal"
args = ["--owner", { from = "inputs.owner", fallback = "unknown" }]
