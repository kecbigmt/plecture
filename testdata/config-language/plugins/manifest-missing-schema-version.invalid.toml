# plect-fixture: result=invalid layer=structural diagnostic=PLECTURE-CFG-FIELD-REQUIRED entry=plugin
# reason: schema_version is required so a manifest's format is identifiable before anything else is read.

version           = "0.1.0"
plect_min_version = "0.0.0"
description       = "A manifest with no schema_version."
