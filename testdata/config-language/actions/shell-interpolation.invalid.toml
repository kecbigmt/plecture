# plect-fixture: result=invalid layer=structural diagnostic=PLECT-CFG-SHELL-INTERPOLATION entry=definitions
# reason: shell source is literal, so a Plecture or CEL interpolation inside script is rejected.

[runtime]
kind = "task"

[runtime.setup]
type = "shell"
script = '''
echo {{.SessionName}}
'''
