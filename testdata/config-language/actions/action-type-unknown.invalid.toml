# plect-fixture: result=invalid layer=structural diagnostic=PLECT-CFG-ACTION-TYPE-UNKNOWN entry=definitions
# reason: exec and shell are the only action variants.

[runtime]
kind = "task"

[runtime.setup]
type = "python"
script = "print('hi')"
