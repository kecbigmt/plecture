# plect-fixture: result=valid entry=definitions
# reason: a computed nested output binding produces a string and does not write through.

[labelled_runtime]
kind = "effect"

[labelled_runtime.inner]
uses = "official.claude.runtime"

[labelled_runtime.outputs.bind]
pid   = { from = "inner.outputs.pid" }
label = { expr = "'pid-' + string(inner.outputs.pid)" }

[labelled_runtime.outputs_schema]
type = "object"

[labelled_runtime.outputs_schema.properties]
pid   = { type = "integer", mutable = true }
label = { type = "string" }
