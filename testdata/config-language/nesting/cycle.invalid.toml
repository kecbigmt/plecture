# plect-fixture: result=invalid layer=semantic diagnostic=PLECTURE-CFG-NESTING-CYCLE entry=definitions
# reason: a nesting chain that reaches itself has no innermost layer.

[outer_runtime]
kind = "task"

[outer_runtime.inner]
uses = "middle_runtime"

[middle_runtime]
kind = "task"

[middle_runtime.inner]
uses = "outer_runtime"
