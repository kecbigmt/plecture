# plect-fixture: result=invalid layer=semantic diagnostic=PLECT-CFG-FROM-ROOT entry=definitions
# reason: a nested outputs binding observes only inner outputs and this layer's locals.

[labelled_runtime]
kind = "task"

[labelled_runtime.inner]
uses = "official.claude.runtime"

[labelled_runtime.outputs.bind]
owner = { from = "session.inputs.owner" }

[labelled_runtime.outputs_schema]
type = "object"

[labelled_runtime.outputs_schema.properties]
owner = { type = "string" }
