# plect-fixture: result=invalid layer=structural diagnostic=PLECTURE-CFG-REF-DYNAMIC entry=definitions
# reason: nested-task selection is static topology and is never computed.

[outer_runtime]
kind = "task"

[outer_runtime.inner]
uses = { from = "inputs.runtime" }
