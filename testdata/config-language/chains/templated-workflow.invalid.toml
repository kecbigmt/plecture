# plect-fixture: result=invalid layer=structural diagnostic=PLECT-CFG-REF-DYNAMIC entry=definitions
# reason: a chain's workflow reference is static, so templated selection is not part of the language.

[pursue_goal]
kind = "task"

[pursue_goal.setup]
type = "exec"
bin  = "okf-goal"
args = ["task", "validate-goal-resource"]

[[pursue_goal.chains]]
id        = "review"
workflow  = { from = "inputs.review_workflow" }
placement = "sibling"

[pursue_goal.chains.when]
all = [{ judge_pending = "goal-met" }]
