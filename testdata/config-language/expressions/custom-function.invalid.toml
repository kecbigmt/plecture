# plect-fixture: result=invalid layer=cel diagnostic=PLECT-CFG-CEL-CUSTOM-FUNCTION entry=definitions
# reason: the profile declares no custom functions, so Plecture capabilities stay structural rather than becoming CEL calls.

[notify]
kind    = "channel"
type    = "exec"
command = "curl"
args    = [{ expr = "bin('codex-exec-enqueue')" }]
