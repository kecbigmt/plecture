# plect-fixture: result=invalid layer=cel diagnostic=PLECTURE-CFG-CEL-UNKNOWN-NAME entry=definitions
# reason: workflow is not a visible variable on a channel argument surface.

[notify]
kind    = "channel"
type    = "exec"
command = "curl"
args    = [{ expr = "workflow.outputs.branch" }]
