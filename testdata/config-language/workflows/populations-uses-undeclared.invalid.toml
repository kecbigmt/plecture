# plect-fixture: result=invalid layer=semantic diagnostic=PLECTURE-CFG-POPULATION-CONTRACT entry=definitions
# reason: a population may select only a query means its resolved observer actually declares.

[bad_uses_undeclared]
kind               = "workflow"
workspace_provider = "query_provider"

[[bad_uses_undeclared.populations]]
name              = "dispatch"
resource_observer = "subscribe_only_source"
uses              = ["poll"]
poll_every        = "5m"

[bad_uses_undeclared.populations.query]
scope = "open"
