# plect-fixture: result=invalid layer=structural diagnostic=PLECTURE-CFG-FIELD-UNKNOWN entry=definitions
# reason: a chain fires off a task document's instances, so it is not part of the effect grammar.

[render]
kind = "effect"

[render.setup]
type = "exec"
bin  = "github-issue-pr"
args = ["render-instruction"]

[[render.chains]]
id        = "review"
workflow  = "review_session"
placement = "sibling"

[render.chains.when]
all = [{ judge_pending = "ac-met" }]
