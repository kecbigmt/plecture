# plect-fixture: result=invalid layer=semantic diagnostic=PLECTURE-CFG-SCHEMA-VERSION-OLDER entry=config
# reason: the tree declares dialect 1 after dialect 2 superseded it, so the error directs the operator to a migration.

schema_version = 1

workspace_dirs_root = "~/worktrees"
