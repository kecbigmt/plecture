# plect-fixture: result=invalid layer=structural diagnostic=PLECTURE-CFG-FIELD-UNKNOWN entry=config
# reason: an unknown field is a load error rather than being silently ignored.

schema_version = 1

workspace_dirs_root = "~/worktrees"
worktrees_root      = "~/worktrees"
