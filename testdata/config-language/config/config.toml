# plect-fixture: result=valid entry=config
# reason: config.toml is a reserved root file carrying machine-wide settings, not definition blocks.

schema_version = 2

workspace_dirs_root = "~/worktrees"
max_up_children     = 12
resource_allowlist  = ["^https://github\\.com/kecbigmt/"]
plugin_dirs         = ["~/.config/plect/plugins"]
channels            = ["notify"]

[inputs_schema]
type                 = "object"
additionalProperties = false

[inputs_schema.properties]
task = { type = "string" }
