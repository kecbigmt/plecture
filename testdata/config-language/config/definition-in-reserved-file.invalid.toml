# plect-fixture: result=invalid layer=structural diagnostic=PLECT-CFG-FIELD-UNKNOWN entry=config
# reason: a reserved root file is not a definition file, so a definition table cannot appear in it.

workspace_dirs_root = "~/worktrees"

[runtime]
kind = "task"
